import React from 'react'
import {Segement, Image, Grid, Divider, Header, Button, List } from "semantic-ui-react"
import {followUser, unfollowUser} from '../../util/profileActions'

const ProfileHeader = () => {
  return (
    <div>ProfileHeader</div>
  )
}

export default ProfileHeader